const arr = [1,2,4,5,6,7,9,10];
let missingInt = [];

for(let i = 0; i < arr.length-1; i++){
    let next = arr[i] + 1;
    
    if(!(arr.includes(next))) {
        missingInt.push(next)
    }
}
console.log(missingInt);

Output -
[ 3, 8 ]
